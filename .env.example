# OpenAI API Configuration
OPENAI_API_KEY=your-api-key-here
OPENAI_MODEL=gpt-5.2

# Alternative models you can use:
# OPENAI_MODEL=gpt-5.2 (latest - requires preview access)
# OPENAI_MODEL=gpt-4o (recommended - has vision + latest features)
# OPENAI_MODEL=gpt-4-turbo (also supports vision)
# OPENAI_MODEL=gpt-4 (older, still good)

# Carousel Settings
MAX_SLIDES=6

# Image Search (optional - leave empty to search entire web)
# ALLOWED_IMAGE_DOMAINS=example.com,other.com

# Image Download Timeout (seconds)
IMAGE_DOWNLOAD_TIMEOUT=12
